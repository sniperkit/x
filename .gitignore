# gitignore - Specifies intentionally untracked files to ignore
# https://git-scm.com/docs/gitignore

# This file should only specify rules for artifacts generated by the project.
# For locally-generated files such as those created by your editor, consider
# either a global .gitignore or an explicit exclude rule in .git/info/exclude
# https://help.github.com/articles/ignoring-files/

# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.txt
